// skillbridge-mvp/client/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Any other original configurations you had...
  // e.g. images, rewrites, etc.
};

module.exports = nextConfig;